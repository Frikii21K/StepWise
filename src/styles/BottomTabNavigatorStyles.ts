// src/styles/BottomTabNavigatorStyles.ts
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    backgroundColor: '#292929',
    padding: 10,
    alignItems: 'flex-start', 
    borderBottomWidth: 1,
    borderColor: '#444',
  },
  headerText: {
    fontSize: 20,
    color: '#fff',
    fontWeight: 'bold',
  },
  headerDate: {
    fontSize: 16,
    color: '#ccc',
  },
});
